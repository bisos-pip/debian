#+title: Work Plan
#+DATE: <2026-06-10 Wed 13:54>
#+AUTHOR: Mohsen BANAN
#+OPTIONS: toc:4
#+TAGS: me ai aiAndMe

# Provenance (installed once by aiActivity.cs --- not regenerated):
#   Date:      2026-08-05T11:22:41-07:00
#   User:      bystar
#   Templates: /bisos/apps/defaults/ai-templates
#   Command:   /bisos/pipx/bin/aiActivity.cs -i initiate --activity=bisos-pip

####+BEGIN: b:ai:file/particulars
* AI Working Context
  Working Directory: /bisos/git/auth/bxRepos/bisos-pip/debian
  File: /bisos/git/auth/bxRepos/bisos-pip/debian/AI-WorkPlan.org
  Activity: bisos-pip
  Companion Docs: AI-WORKFLOW.org, AI-Activity.org, AI-DevStatus.org, README.org
###+END:

This Work Plan file drives the AI collaborative development.
Interactions are broken down into "Stages". Each Stage has a set
of TODOs that I work with Claude to accomplish.

Note to a fresh Claude: read this file together with AI-WORKFLOW.org,
AI-Activity.org and AI-DevStatus.org to orient yourself. The =<<Context Confirmation>>=
TODO below is the standard first step, or use =/bx-confirm0= as the automated equivalent.
TODOs tagged =:ai:= are assigned to Claude. After completing a TODO, summarize results
under =----- AI Results: ------= and mark it DONE.

* Stage 0: Confirm AI's Understanding of This Repo's Context

Goal: Confirm AI's understanding of the context and capture that in [[Context Confirmation]] TODO.


* Stage 0 TODOs
** +
** TODO [#A] <<Context Confirmation>> for AI (typically triggered by /bx-confirm0) :aiAndMe:
SCHEDULED: <2026-06-09 Tue>
Must be repeated each time a new Claude instance is started here.
Based on the series of files starting from CLAUDE.md in this repo,
describe succinctly what you have understood.
Enumerate explicitly which files you have imported and which you have considered.
Prompt the user to confirm that the desired/appropriate model (/model) is being used for this project.
----- AI Results: ------
** -

* Stage 1: Extend systemd_seedInfo with preFunc/postFunc hooks

Goal: Add optional =preFunc= and =postFunc= callables to =SystemdSeedInfo= and
=setup()= in =systemd_seedInfo.py=, and call them at the appropriate points in
=bifSystemd_csu.py= (before and after enable+start). This gives =*-sysd.pcs=
files a Python-native place to run setup/teardown tied to the service lifecycle,
without needing a separate oneshot unit.

Context: discussed during airflow capability work in =/bisos/asc/web/bin=.
The use case is pre/post execution of preparations used by =ExecStart= — not
rc.local-style side effects. The hooks are called at =sysdUnit_enable= time
(Python-level), not by systemd itself.


* Stage 1 TODOs
** +
** DONE [#A] Add preFunc/postFunc hooks to systemd_seedInfo and bifSystemd_csu :ai:
CLOSED: [2026-08-05 Wed]
SCHEDULED: <2026-08-05 Wed>
In =py3/bisos/debian/systemd_seedInfo.py=:
- Add =preFunc: typing.Callable | None = None= and =postFunc: typing.Callable | None = None=
  fields to =SystemdSeedInfo= (with property + setter).
- Add same parameters to =setup()=.

In =py3/bisos/debian/bifSystemd_csu.py=:
- In the enable+start command (=sysdUnit_enable= or equivalent), call
  =systemdSeedInfo.preFunc()= before enabling/starting and
  =systemdSeedInfo.postFunc()= after, when they are not =None=.

The hooks receive no arguments — the =.pcs= file closes over whatever it needs.
----- AI Results: ------
=systemd_seedInfo.py=: added =preFunc= and =postFunc= fields, properties, setters, and =setup()= params to =SystemdSeedInfo=.
=bifSystemd_csu.py=: added pre/post hook calls to =SysUnit.ensure()= only (not =UserUnit= or =SocketUnit=). Hooks are called at Python level — before =serviceFileVerify/reload/restart/enable= and after, respectively.
** -


# Local Variables:
# eval: (setq-local toc-org-max-depth 4)
# eval: (setq-local org-log-done 'time)
# End:
